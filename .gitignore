node_modules
.next
out
build
dist

# Secrets — never commit
.env
.env.local
.env*.local

# 0G Storage cache files (large, not needed in repo)
data/storage/

# Per-employer operator private keys (generated at deploy time, never committed)
data/tenants/*/operator.key

# Compiled contract artifacts (regenerated by npm run compile)
contracts/out/

# TypeScript build cache
tsconfig.tsbuildinfo

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db
